<h1><%= @project.name %></h1>
<label>Description: </label>
<p><%= @project.description%></p>
<ul>
    <% @project.devs.each do |dev| %>
    <li><%= link_to dev.name, dev_path(dev) %></li>
    <% end %>
</ul>


<%= link_to "Remove project", project_path(@project), method: "DELETE" %><br>
<%= link_to "Project list.", projects_path %>

